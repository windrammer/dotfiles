

# Please enter the commit message for your changes. Lines starting
# with '#' will be ignored, and an empty message aborts the commit.

# Expected commit message format: type(scope): subject
# Examples: feat: add new feature        (minor bump)
#           fix(auth): resolve login bug  (patch bump)
#           break: remove deprecated API  (major bump)
#
# Valid types: feat, fix, break, docs, style, refactor,
#              perf, test, build, ci, chore, revert, wip
#
# Version bumps: fix → patch, feat → minor, break → major
#
# To bypass: prefix message with 'wip:'
#
# 🏗️ Use `git pushbuild` to push and trigger a Woodpecker CI build

