# linux/ — dormant

This directory is not currently used. Only `mac/` is actively maintained and wired
into the Ansible playbook / `link.sh`. The contents here are historical; do not
assume they reflect current preferences.
